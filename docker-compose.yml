version: '3.8'

services:
  unified-platforms:
    image: ghcr.io/upanalytics2788/unified-platforms-gtm-os:latest
    container_name: unified-platforms-app
    restart: always
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      # - GEMINI_API_KEY=${GEMINI_API_KEY}
      # Add other environment variables as needed
